# 项目进度

## 当前阶段

公众号文章与发表详情导出。

## 已完成

- 支持按公众号发表记录导出文章正文。
- 支持将阅读量、点赞量、分享量、留言量、转载量等字段写入 `summary.csv`、`summary.json` 和文章 Markdown。
- 支持自动保留发表详情记录中的非空字段，避免后台字段扩展后丢失数据。
- 支持读取文章分析页，保存核心分析指标、按日来源明细、文章内跳转统计和音频明细。

## 最近验证

- 已完成 Python 语法检查。
- 已完成模拟发布详情记录的汇总与 Markdown 输出一致性验证。
- 已完成真实文章分析页验证：核心分析字段可解析，按日明细 38 条，文章内跳转统计 5 条。

## 待办

- 已完成全量导出：75 条 summary 记录、21 个文章分析字段，分析请求错误 0 条。
- 已完成用户画像全量导出：75 条 summary 中，性别、年龄、地域画像列均生成，2 篇文章返回非空画像，其余文章由微信分析页返回空数据。
- 已将文章详情字段表头中文化，例如广告信息、文章合集信息、留言状态和封面图片等。

## 本次审查

- 已修复分析页缺少可选 `detailData` 时丢失核心分析字段的问题。
- 已修复列表模式缺少文章分析 ID 时仍请求分析页的问题。
- 已增加 `summary.csv` / `summary.json` 写入预检，文件被 Excel 占用时提前终止，避免生成部分结果。
- 本次修改已通过 Python 语法检查、差异格式检查和分析页缺少画像字段的回归测试；未重新执行全量网络导出。
